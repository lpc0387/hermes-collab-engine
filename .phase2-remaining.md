# Remaining 5 changes for 8765 engine optimization

Apply each change below in order.

## 1. agents.py - Add _opencode_session_close and track sends
After line 395 `_o.session_send = _opencode_session_send`, add:
```
_o.close_session = _opencode_session_close
```

Replace line 393:
OLD: `    handle.proc = _sp.Popen(cmd, stdin=_sp.DEVNULL, stdout=_sp.DEVNULL, stderr=_sp.DEVNULL)`
NEW: `    _proc = _sp.Popen(cmd, stdin=_sp.DEVNULL, stdout=_sp.DEVNULL, stderr=_sp.DEVNULL)`

Add function BEFORE _opencode_session_send (line 386):
```
def _opencode_session_close(handle: SessionHandle) -> None:
    import subprocess as _sp
    for _proc in handle.meta.get("_pending_sends", []):
        if _proc.poll() is None:
            _proc.kill()
    if handle.proc and handle.proc.poll() is None:
        handle.proc.kill()
```

In _opencode_session_send, after the Popen line, add:
```
    _pending = handle.meta.setdefault("_pending_sends", [])
    _pending.append(_proc)
```

## 2. agents.py - get_backend auto fallback
Replace lines 518-522:
OLD:
```
def get_backend(name: str) -> AgentBackend:
    if name not in _BUILTINS:
        raise KeyError(f"Unknown agent backend: {name!r}. Available: {sorted(_BUILTINS.keys())}")
    return _BUILTINS[name]
```
NEW:
```
def get_backend(name: str) -> AgentBackend:
    if name not in _BUILTINS:
        raise KeyError(f"Unknown agent backend: {name!r}. Available: {sorted(_BUILTINS.keys())}")
    backend = _BUILTINS[name]
    if not backend.is_available():
        import logging
        logging.getLogger(__name__).warning(
            f"Agent {name!r} is not available on PATH, falling back to 'opencode'"
        )
        return _BUILTINS.get("opencode", backend)
    return backend
```

## 3. engine.py - ANTHROPIC_BASE_URL default removal
Replace lines 1710-1711:
OLD:
```
            _ANTHROPIC_DEFAULTS: dict[str, str] = {
                "ANTHROPIC_BASE_URL": "https://api.anthropic.com",
            }
```
NEW:
```
            _ANTHROPIC_DEFAULTS: dict[str, str] = {}
```

## 4. engine.py - _worker_sessions locking
Replace lines 265-277:
OLD:
```
                for node_id, handle in list(self._worker_sessions.items()):
                    try:
                        self.agent_backend.close_session(handle)
                    except Exception:
                        pass
                    if handle.session_id:
                        try:
                            self.store.save_agent_session(session_id, handle.session_id)
                        except Exception:
                            pass
                    with self._worker_procs_lock:
                        self._worker_sessions.clear()
                        self._worker_procs.clear()
```
NEW:
```
                with self._worker_procs_lock:
                    for node_id, handle in list(self._worker_sessions.items()):
                        try:
                            self.agent_backend.close_session(handle)
                        except Exception:
                            pass
                        if handle.session_id:
                            try:
                                self.store.save_agent_session(session_id, handle.session_id)
                            except Exception:
                                pass
                    self._worker_sessions.clear()
                    self._worker_procs.clear()
```

## 5. config_store.py - delete _DEFAULT_MAX_KEEP
DELETE lines 36-37:
```
_DEFAULT_MAX_KEEP = 5
"""Default number of backup files to retain."""
```

## 6. store.py - CURRENT_TIMESTAMP regex
In _execute (line 358), _query (line 365), _one (line 370):
Replace `sql.replace("CURRENT_TIMESTAMP", "datetime('now','localtime')")`
With `re.sub(r'\bCURRENT_TIMESTAMP\b', "datetime('now','localtime')", sql)`

Also add `import re` at the top of the file if not present.

## 7. add_agent.py - URL building with urlparse
Add `from urllib.parse import urlparse, urlunparse` with other imports at top.

Replace lines 83-88:
OLD:
```
    url = base_url.rstrip('/')
    if not url.endswith('/chat/completions'):
        if '/v1' not in url:
            url += '/v1/chat/completions'
        else:
            url += '/chat/completions'
```
NEW:
```
    url = base_url.rstrip('/')
    if not url.endswith('/chat/completions'):
        parsed = urlparse(url)
        path = parsed.path.rstrip('/')
        if '/v1' not in path:
            path += '/v1/chat/completions'
        else:
            path += '/chat/completions'
        parsed = parsed._replace(path=path)
        url = urlunparse(parsed)
```

## Verification
After all changes, run:
cd /root/hermes-collab-engine && python3 -c "from src.hermes_collab_engine.agents import get_backend; from src.hermes_collab_engine.engine import CollabEngine; from src.hermes_collab_engine.store import CollabStore; print('ALL IMPORTS OK')"
