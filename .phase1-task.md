# Phase 1: 5 项小改动 + Phase 2: 8 项改动

## 改动清单（13 项，按文件分组）

### 文件: agents.py

**1. opencode capabilities 补全 (line 311)**
旧:
```
    capabilities=["file-edit", "git-ops"],
```
新:
```
    capabilities=["file-edit", "git-ops", "test-run", "mcp-host", "search"],
```

**2. claude-code output_format_flags 清空 (line 282)**
旧:
```
    output_format_flags=["--output-format", "json"],
```
新:
```
    output_format_flags=[],
```

**3. opencode fd 泄漏修复 (lines 358-359)**
旧:
```
    if not stdout_path:
        _stdout.close() if hasattr(_stdout, 'close') else None
```
新:
```
    if stdout_path:
        _stdout.close()
    if stderr_path:
        _stderr.close()
```

**4. opencode session list 加日志 (line 378-379)**
旧:
```
    except Exception:
        pass
```
新:
```
    except Exception:
        import logging as _log
        _log.getLogger(__name__).warning("opencode session list failed", exc_info=True)
```

**5. hermes `--provider` 不硬编码 (line 397)**
旧:
```
    command=["hermes", "--provider", "opencode-go"],
```
新:
```
    command=["hermes"],
```

**6. opencode _session_send 跟踪子进程 (lines 383-391)**
旧:
```
def _opencode_session_send(handle: SessionHandle, message: str) -> None:
    import subprocess as _sp
    sid = _opencode_ensure_session_id(handle)
    cmd = ["opencode", "run", "-s", sid, message]
    model = handle.meta.get("model", "")
    if model:
        cmd.extend(["--model", model])
    _sp.Popen(cmd, stdin=_sp.DEVNULL, stdout=_sp.DEVNULL, stderr=_sp.DEVNULL)
```
新:
```
def _opencode_session_send(handle: SessionHandle, message: str) -> None:
    import subprocess as _sp
    sid = _opencode_ensure_session_id(handle)
    cmd = ["opencode", "run", "-s", sid, message]
    model = handle.meta.get("model", "")
    if model:
        cmd.extend(["--model", model])
    _proc = _sp.Popen(cmd, stdin=_sp.DEVNULL, stdout=_sp.DEVNULL, stderr=_sp.DEVNULL)
    _pending = handle.meta.setdefault("_pending_sends", [])
    _pending.append(_proc)

def _opencode_session_close(handle: SessionHandle) -> None:
    import subprocess as _sp
    for _proc in handle.meta.get("_pending_sends", []):
        if _proc.poll() is None:
            _proc.kill()
    if handle.proc and handle.proc.poll() is None:
        handle.proc.kill()
```

**7. get_backend 自动 fallback (lines 515-519)**
旧:
```
def get_backend(name: str) -> AgentBackend:
    if name not in _BUILTINS:
        raise KeyError(f"Unknown agent backend: {name!r}. Available: {sorted(_BUILTINS.keys())}")
    return _BUILTINS[name]
```
新:
```
def get_backend(name: str) -> AgentBackend:
    if name not in _BUILTINS:
        raise KeyError(f"Unknown agent backend: {name!r}. Available: {sorted(_BUILTINS.keys())}")
    backend = _BUILTINS[name]
    if not backend.is_available():
        import logging
        logging.getLogger(__name__).warning(
            f"Agent {name!r} is not available on PATH, falling back to 'opencode'"
        )
        return _BUILTINS.get("opencode", backend)
    return backend
```

### 文件: engine.py

**8. ANTHROPIC_BASE_URL 默认值删除 (lines 1710-1711)**
旧:
```
            _ANTHROPIC_DEFAULTS: dict[str, str] = {
                "ANTHROPIC_BASE_URL": "https://api.anthropic.com",
            }
```
新:
```
            _ANTHROPIC_DEFAULTS: dict[str, str] = {}
```

**9. _worker_sessions 加锁 (lines 265-277)**
旧:
```
                for node_id, handle in list(self._worker_sessions.items()):
                    try:
                        self.agent_backend.close_session(handle)
                    except Exception:
                        pass
                    if handle.session_id:
                        try:
                            self.store.save_agent_session(session_id, handle.session_id)
                        except Exception:
                            pass
                    with self._worker_procs_lock:
                        self._worker_sessions.clear()
                        self._worker_procs.clear()
```
新:
```
                with self._worker_procs_lock:
                    for node_id, handle in list(self._worker_sessions.items()):
                        try:
                            self.agent_backend.close_session(handle)
                        except Exception:
                            pass
                        if handle.session_id:
                            try:
                                self.store.save_agent_session(session_id, handle.session_id)
                            except Exception:
                                pass
                    self._worker_sessions.clear()
                    self._worker_procs.clear()
```

### 文件: config_store.py

**10. 删除 _DEFAULT_MAX_KEEP (line 36-37)**
旧:
```
_DEFAULT_MAX_KEEP = 5
"""Default number of backup files to retain."""

```
新:
```
# 删除这两行
```

### 文件: store.py

**11. CURRENT_TIMESTAMP 正则替换 (lines 357-373)**
在文件顶部 import re 后，将 _execute, _query, _one 中的 replace 改为 re.sub
旧:
```
    def _execute(self, sql: str, params: tuple = ()):
        sql = sql.replace("CURRENT_TIMESTAMP", "datetime('now','localtime')")
```
新:
```
    def _execute(self, sql: str, params: tuple = ()):
        sql = re.sub(r'\bCURRENT_TIMESTAMP\b', "datetime('now','localtime')", sql)
```

同样修改 _query 和 _one (lines 365, 370)。

### 文件: add_agent.py

**12. URL 构建用 urlparse (lines 83-88)**
旧:
```
    url = base_url.rstrip('/')
    if not url.endswith('/chat/completions'):
        if '/v1' not in url:
            url += '/v1/chat/completions'
        else:
            url += '/chat/completions'
```
新:
```
    from urllib.parse import urlparse, urlunparse
    url = base_url.rstrip('/')
    if not url.endswith('/chat/completions'):
        parsed = urlparse(url)
        path = parsed.path.rstrip('/')
        if '/v1' not in path:
            path += '/v1/chat/completions'
        else:
            path += '/chat/completions'
        parsed = parsed._replace(path=path)
        url = urlunparse(parsed)
```

### 文件: server.py

**13. distill cronjob 注册 (在 main() 中追加)**
在 server.py 的 main 函数末尾，启动 HTTP 之前，添加：
```
    # Register daily distill cronjob (best-effort)
    try:
        from hermes_collab_engine.distill.daily_distill import run_daily_distill
        _log.info("Distill module available — run via cron or direct call")
    except ImportError:
        _log.info("Distill module not available")
```

## 验证要求
- 改完后 `cd /root/hermes-collab-engine && python3 -c "from src.hermes_collab_engine.agents import get_backend; print('OK')"` 必须成功
- `python3 -c "from src.hermes_collab_engine.engine import CollabEngine; print('OK')"` 必须成功
- `python3 -c "from src.hermes_collab_engine.store import CollabStore; print('OK')"` 必须成功
- `cd /root/hermes-collab-engine && python3 -m hermes_collab_engine.cli doctor` 必须成功
